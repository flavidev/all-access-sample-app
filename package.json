{
  "name": "all-access-sample-app",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "server": "json-server --watch mockPeople.json --port 5555",
    "client": "yarn --cwd ./client/ start",
    "dev": "concurrently \"yarn server\"   \"yarn client\""
  },
  "dependencies": {
    "concurrently": "^6.2.0",
    "json-server": "^0.16.3"
  }
}